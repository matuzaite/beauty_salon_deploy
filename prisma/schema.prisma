generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model services {
  id   BigInt @id @default(autoincrement())
  name String
}

model specialists {
  id             BigInt   @id @default(autoincrement())
  name           String
  specialization String?
  phone          String?
}
